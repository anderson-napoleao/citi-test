version: '3.9'

services:
  api-gateway:
    build: bank-api-gateway
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    ports:
      - "8443:8443"

  eureka:
    build: bank-service-registry
    ports:
      - "8761:8761"
